import 'package:flutter/material.dart';

class ScheduledAppointmentScreen extends StatelessWidget {
  const ScheduledAppointmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
