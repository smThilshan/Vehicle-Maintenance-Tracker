import 'package:flutter/material.dart';

class PartsScreen extends StatelessWidget {
  static const routeName = 'parts_screen';

  const PartsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
